# Production secrets are NOT stored in this repo.
#
# Run optimizer-tools from the clickholes repo (Compose mounts secrets + sites.json):
#   clickholes/mise/containers/optimizer-tools/env/production.env
#
# Decrypt via clickholes RoRo workflow when needed. See clickholes
# mise/containers/optimizer-tools/README.md.
